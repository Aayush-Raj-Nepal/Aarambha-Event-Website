/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig
// define host name for image loading
module.exports = {
  images: {
    domains: ['images.unsplash.com'],
  },
}
